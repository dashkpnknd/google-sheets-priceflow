# Ульяновск: текущий рабочий вариант

Этот каталог — сохранённый пакет текущего развёртывания без НОБЕЛЬ.РФ.
Его файлы не меняются при доработке `../nobel`.

Для отката в Apps Script загрузить из этого каталога одноимённые файлы:
`TelegramCatalog.gs`, `PriceFlowAvitoMatcher.gs`, `PriceFlowTemplateMatcher.gs`
и `TelegramCatalogSidebar.html`.
