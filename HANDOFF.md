# PriceFlow — передача проекта

Этот репозиторий содержит Google Apps Script-продукты для автоматической выгрузки ассортимента и цен в клиентские Google Таблицы.

## Обязательное правило работы

Нельзя самостоятельно придумывать, расширять или изменять бизнес-правила, ценовые диапазоны, исключения, маршрутизацию или формат данных. Выполнять только явно сформулированные пользователем требования. Если для случая нет прямого правила, остановиться и запросить уточнение; не подставлять «логичное» предположение. После каждого изменения обновлять этот `HANDOFF.md` и передавать пользователю готовый файл кода для копирования в Apps Script.

## Рабочие клиентские реализации

Это не единый обезличенный продукт: в репозитории есть отдельные клиентские реализации. Не переносить правила одного клиента в другой без проверки.

| Клиентская папка | Интеграция | Источник | Канонические исходники | Что делает |
| --- | --- | --- | --- | --- |
| `Ульяновск/Telegram/` | Telegram | Публичный канал `@opt_uniseil` | `Ульяновск/Telegram/` | Пересобирает каталог поставщика в товарных листах и применяет правила Ульяновска |
| `Москва/МойСклад/` | МойСклад | API МоегоСклада | `Москва/МойСклад/` | Пересобирает каталог из МоегоСклада и применяет правила Москвы |
| `Электросталь/Telegram/` | Telegram | Публичный прайс `@astoredirectprice` | `Электросталь/Telegram/` | Пересобирает каталог поставщика с индивидуальными наценками Электростали |
| `Краснодар/RuStore/` | Telegram (приватный) | `Прайс ru:Store новый` | `Краснодар/RuStore/` | Серверный read-only клиент, HTTPS-снимок и Apps Script-парсер |
| `Орск/RMGroup/` | RM Group Client API | `https://api-c.rmgroup.website/` | `Орск/RMGroup/` | Получает `pricelist.get` и пересобирает каталог после подключения токена |

Папки клиента — единственная точка входа для следующего разработчика: в них находятся код, панель, тесты и клиентские правила. Папка `outputs/` содержит только архивы и ранние прототипы.

### Орск — Яблочный Спас / RM Group

- `Орск/RMGroup/RMGroupCatalog.gs` — Apps Script для RM Group Client API.
- `Орск/RMGroup/RMGroupCatalogSidebar.html` — подключение токена и первая синхронизация.
- `Орск/RMGroup/test-rmgroup-catalog.mjs` — тесты официальной структуры `pricelist.get`.
- `Орск/RMGroup/README.md` — установка.

Техническая часть подготовлена 2026-08-22 без реального токена и без таблицы. Официальный API работает единственным POST-адресом `https://api-c.rmgroup.website/`, заголовок авторизации — `Auth`, запрос — `{"method":"pricelist.get"}`. Прайс доступен с 11:00 до 18:00 МСК; вне этого времени ожидается HTTP 425. Токен при подключении проверяется до сохранения и хранится только в Script Properties. Создаётся ровно один триггер `syncRMGroupCatalog` раз в 15 минут. Поля API `brand`, `category`, `subcategory`, `id`, `name`, `country`, `cost` разобраны в стандартные товарные листы. Ценовая наценка/округление не реализованы намеренно: пользователь пока не задавал правила, поэтому API-цена записывается без изменений. Для запуска нужны только выданный командой RM Group токен и ссылка на Google-таблицу с обычными листами клиента.

Остальные папки и файлы в `outputs/` — ранние прототипы, архивы ZIP или отдельные эксперименты. Не смешивать их с двумя продуктами выше при установке клиенту.

## Структура каждого продукта

### Ульяновск — Telegram

- `TelegramCatalog.gs` — серверная логика Apps Script: загрузка публичного канала, разбор товаров, маршрутизация по листам, автозапуск и отчёт.
- `TelegramCatalogSidebar.html` — боковая панель подключения: магазин/город, канал, кнопки подключения и ручной пересборки.
- `test-telegram-catalog.mjs` — автоматические тесты разбора и маршрутизации.
- `README.md` — краткая установка для таблицы клиента.

Текущий профиль разбора настроен на `@opt_uniseil`. Для канала с другой структурой сообщения меняются только правила разбора в `tcParsePost_`, `tcLine_` и `tcExpand_`; интерфейс и остальные компоненты сохраняются.

Нельзя потерять следующие особенности Ульяновска:

- страна хранится в отдельной колонке вместе с флагом;
- создаётся отдельная строка `2 SIM` по цене `SIM + eSIM`, если в панели включён переключатель;
- применяется наценка из файла правил клиента;
- для одинаковой модели/конфигурации/памяти/цвета выбирается подходящая самая дешёвая позиция поставщика;
- значения записываются текстом, а не требуют совпадения с выпадающими списками исходной таблицы.

### Москва — МойСклад

- `MoySkladCatalog.gs` — серверная логика Apps Script: API МоегоСклада, разбор названий, маршрутизация и автозапуск.
- `MoySkladCatalogSidebar.html` — боковая панель с полем «Магазин | Город» и API-токеном.
- `test-moysklad-catalog.mjs` — автоматические тесты категорий, атрибутов и записи.
- `README.md` — краткая установка.

Нельзя потерять следующие особенности Москвы:

- телефоны Apple и Android распределяются в два товарных блока листа `телефоны`;
- ноутбуки, планшеты, часы, наушники, приставки, Dyson и iMac направляются в соответствующие листы;
- аксессуары (чехлы, стёкла, кабели, зарядки, адаптеры, держатели, ремешки, powerbank и игровые аксессуары) направляются в `аксессуары`, а реальные телефоны остаются в `телефоны`;
- EarPods/AirPods и другие наушники остаются на листе `наушники`;
- токен хранится только в `Script Properties`, не в ячейках и не в репозитории;
- после подключения устанавливается триггер раз в 15 минут.

### Краснодар — RuStore

- `RuStoreCatalog.gs` — Apps Script: получает защищённый JSON-снимок прайс-постов и пересобирает товарные листы раз в 15 минут.
- `RuStoreCatalogSidebar.html` — панель настройки адреса HTTPS-снимка и секрета; секрет хранится только в Script Properties.
- `test-rustore-catalog.mjs` — тесты парсинга, стран/SIM, цен, пропусков и маршрутизации.
- `README.md` — установка и контракт серверного снимка.

На листе `телефоны` Краснодар использует два существующих блока: левый — iPhone, правый — все Android-телефоны. Все распознанные Android-бренды (`Samsung/Galaxy`, Pixel, Xiaomi/Redmi, Honor, Huawei, OnePlus, realme, OPPO, vivo) направляются вправо. Другие товарные листы не меняются.

У актуального Samsung-поста `Прайс ru:Store новый` заголовок начинается с `🤖 SAMSUNG`. Эмодзи `🤖` обязательно удаляется до распознавания заголовка; иначе строки `S26 …` теряют марку, попадают в `прочее` и не записываются в правый Android-блок. Строки с `?` после цены по-прежнему не записываются.

Краснодарский источник — единственный приватный канал read-only Telegram-аккаунта: **Прайс ru:Store новый**. Это фактическое точное имя, полученное из списка диалогов авторизованного Telegram-аккаунта. Apps Script не входит в Telegram и не хранит Telegram-сессию. Серверный клиент каждые 15 минут перечитывает до 1000 сообщений (включая старые отредактированные прайс-посты) и отдаёт Apps Script JSON вида `{"posts":[{"id":"…","text":"…"}]}` по HTTPS с заголовком `X-PriceFlow-Secret`.

Подготовлен `Краснодар/RuStore/server/` для одноразовой QR-авторизации Telegram. До показа QR нужны только `TG_API_ID` и `TG_API_HASH` от Telegram API-приложения; они передаются контейнеру только при запуске и не записываются ни в Git, ни в файл на сервере. Сессия хранится только в Docker volume сервера.

Для Краснодара нет наценки, округления, схемы «2 SIM» или выбора самой дешёвой позиции. Цена из товарной строки записывается исходным числом в формате `0`. Поддерживается формат «название — цена»; строки объёмной цены `1 шт / 3+ / 5+` намеренно пропускаются, поскольку это правило другого клиентского проекта. Строки без цены или с `?` после цены не становятся товарами и пропускаются только при текущей пересборке; если поставщик затем добавит подтверждённую цену, та же отредактированная строка будет добавлена. Сохраняются все флаги и страны; `🇪🇺` записывается как `Европа 🇪🇺`.

### Электросталь — Telegram

- `TelegramCatalog.gs`, `TelegramCatalogSidebar.html`, `test-telegram-catalog.mjs`, `README.md` — отдельная реализация, не смешивать с Ульяновском.
- Источник — текстовый прайс `@astoredirectprice`; флаг и страна сохраняются в отдельной колонке.
- Apple: 5–15 тыс. +3 000; 16–35 +4 000; 36–69 +5 000; 70–89 +6 000; 90–110 +7 500; 111–150 +9 000; 151–225 +13 000 ₽.
- Samsung/Android: 5–15 тыс. +3 000; 16–35 +5 000; 36–69 +7 000; 70–89 +8 000; 90–110 +8 500; 111–150 +10 000; 151–225 +15 000 ₽.
- **Все остальные категории** используют шкалу Samsung/Android. После любой наценки цена округляется вверх до ближайших 500 ₽ и записывается числом в формате `0`.
- Цены ниже 5 тыс. ₽ не получают наценку. Для цены выше 225 тыс. ₽ применяется `+15 000` для любой категории, затем округление вверх до 500 ₽. Пометки поставщика `(Active)` и `(Уценка)` сохраняются в начале названия, даже если они стоят перед `iPhone`.
- Актуальный формат `@astoredirectprice`: статусы поставщик пишет в конце строки без скобок — `Актив` и `Уценка`; в таблицу они записываются в начале названия как `(Актив)` и `(Уценка)`. Заголовок раздела может быть `Apple · iPhone …`; его нужно распознавать как контекст iPhone.

## Стандартные клиентские листы

Обычный набор листов:

`телефоны`, `аксессуары`, `макбуки`, `аймаки`, `айпады`, `часы`, `наушники`, `пс`, `дайсон`.

В Telegram-версии ассортимент пишется в товарные листы, не создавая отдельный лист с сырой выдачей. В версии МоегоСклада действует то же правило.

## Единый формат цветов для автозагрузки Avito

Во всех клиентских продуктах (`Ульяновск`, `Москва`, `Электросталь`, `Краснодар`, `Орск`) значение записывается в отдельную колонку `Color`/`Цвет` только в формате эталонной автозагрузки Avito. Разрешены исключительно: `бежевый`, `белый`, `голубой`, `зеленый`, `золотистый`, `желтый`, `красный`, `оранжевый`, `розовый`, `серебристый`, `серый`, `синий`, `фиолетовый`, `черный`. Название товарной строки поставщика не менять.

Соответствие определяется по модели и её вариантам в эталонной автозагрузке, а не только по схожести названий: например, `Lavender` у iPhone 17 — `фиолетовый`, `Sage` — `зеленый`, `Natural Titanium` у iPhone 16 Pro — `серый`, `Desert Titanium` — `золотистый`, а синий оттенок iPhone 17 Pro — `синий`, не `голубой`. Фирменные названия, которых ещё нет в утверждённом соответствии, не заменять по догадке: сначала сверить с моделью в эталонной автозагрузке и при отсутствии однозначного варианта запросить решение пользователя.

## Орск — формат ответа RM Group

`pricelist.get` у RM Group может вернуть как прежние блоки `{items:[...]}`, так и актуальный прямой массив товаров в `result`. Обработчик обязан поддерживать оба формата; иначе при действующем токене панель показывает ложные `0 позиций`. Сверка 23 августа 2026: актуальный ответ содержит 929 товаров, из которых 816 распределяются по стандартным листам.

В названиях Samsung использовать полную модель из `subcategory`: например, `Galaxy A57 8/256 Gray`, а не `A57 8/256 Gray`. Память `1TB` и `2TB` у iPhone записывается как `1 ТБ` и `2 ТБ`. В текущем прайсе безопасно распределены Poco/Nothing Phone → `телефоны`, Poco Pad → `айпады`, JBL/Harman/Marshall → `наушники`, Nintendo/Oculus/Steam Deck → `пс`, Mac mini → `макбуки`. Для записи 7 iMac должен существовать лист `аймаки` с шапкой `Title`, `Price`.

Дополнительные листы Орска: `камеры` получает DJI, GoPro, Insta360, Fujifilm и Canon; `колонки` — Яндекс Станции. Ray-Ban и Medicube направляются в `аксессуары`. Сверка текущего прайса после этих правил: все 929 из 929 позиций получают лист назначения.

RM Group не передаёт SIM-конфигурацию отдельным полем и в актуальных названиях телефонов её нет. Оставлять `SimConfig` пустым, пока пользователь не задаст правило по умолчанию; не выводить SIM-конфигурацию по догадке.

## Удаление страны из прайс-листов (24 августа 2026)

Во всех пяти импортёрах — Ульяновск Telegram, Москва МойСклад, Электросталь Telegram, Краснодар RuStore и Орск RM Group — колонка `Country`/`Страна` больше не создаётся и не заполняется. При следующей пересборке скрипт физически удаляет все колонки с таким точным заголовком в каждом листе, затем пересчитывает расположение оставшихся колонок и записывает каталог. Страна может остаться внутренним признаком исходной строки только для разбора или выбора цены, но в Google Sheets не выводится.

## Важные правила безопасности

Никогда не коммитить:

- API-токены МоегоСклада;
- таблицы/экспорты с клиентскими данными;
- скриншоты с токенами;
- `appsscript.json`, если в него случайно добавлены секреты.

В GitHub должны попадать только код, тесты, README и этот handoff.

Никогда не коммитить Telegram `api_id`, `api_hash`, строку сессии, пароль аккаунта, секрет HTTPS-снимка или ссылку-приглашение в закрытый канал. Репозиторий сейчас публичный.

## Проверка перед передачей клиенту

В папке каждого продукта выполнить:

```bash
node test-telegram-catalog.mjs
node test-moysklad-catalog.mjs
```

Команда запускается из соответствующей папки. Также проверять Apps Script:

1. В панели подключить источник и дождаться первого результата.
2. В Apps Script открыть «Триггеры» — должен быть один активный триггер нужного продукта.
3. Открыть «Выполнения» — `syncTelegramCatalog` или `syncMoySkladCatalog` должны завершаться успешно примерно раз в 15 минут.
4. Не оставлять старые триггеры вроде `syncAllProjects`: они создают ложные ошибки и дублирующие запуски.

## Выполненная задача: Москва / МойСклад

Лист `аксессуары` подключён к синхронизации. В `телефоны` попадают только реальные телефоны; чехлы, стёкла, кабели, зарядки, адаптеры, держатели, ремешки, powerbank, док-станции и аксессуары для приставок направляются в `аксессуары`. Наушники, включая AirPods и EarPods, направляются в `наушники`.

Уточнение 23 августа 2026: позиция «Глазурь» (защитное покрытие/стекло), в том числе с текстом «голубая глазурь» или `box`, — аксессуар. Это не название цвета и не подлежит переводу в колонку `Color`. Состояние перед моделью, например `(Active) iPhone 17`, сохраняется в названии и остаётся в левом Apple-блоке листа `телефоны`. Цены из API МоегоСклада приходят в копейках и делятся на 100: телефоны дешевле 1 000 ₽ пропускаются, для аксессуаров текущий порог — от 1 ₽.

Уточнение классификации Samsung: чехлы для AirPods/Buds — `аксессуары`, даже если в названии есть AirPods. Портативные колонки, Galaxy Buds и Galaxy Ring направляются в `наушники`; Galaxy Watch — в `часы`. Позиция Samsung/Galaxy без указанной памяти считается носимым устройством: Watch уже направлен в `часы`, остальные — в `наушники`. Служебная решётка `#` в начале названия товара не выводится в таблицу.

## Текущий следующий шаг: Краснодар / Telegram-клиент

Код Apps Script, парсер, запись в листы, триггер и тесты готовы. Серверный Telegram-сеанс Краснодара успешно авторизован 2026-08-21 через одноразовый код и находится в Docker volume на `/opt/priceflow/krasnodar-telegram`; временный `phone_code_hash` после входа удалён. QR-компонент остаётся как запасной путь, но сейчас не запущен. Сервер `snapshot_server.py` читает только канал `Прайс ru:Store новый` и пересобирает снимок каждые 15 минут. Проверка 2026-08-21 на реальном канале: получено 25 сообщений, разобрано 94 позиции; 11 строк с `?` и 22 строки без цены исключены; у всех 65 позиций с флагами заполнена страна. После авторизации сервис получает только права чтения: не отправляет и не пересылает сообщения, не приглашает людей и не подписывается на другие каналы. Панель Краснодара должна выглядеть как у остальных проектов: в ней нет адресов, каналов, секретов и полей ввода, только кнопки **«Сохранить и выполнить первую синхронизацию»** и **«Пересобрать каталог сейчас»**. Источник уже задан в коде; после копирования двух файлов в Apps Script пользователь нажимает первую кнопку.

Если в панели уже нет полей, но кнопка показывает ошибку «Укажите магазин в формате „Магазин | Город“», значит обновлён только HTML, а `RuStoreCatalog.gs` остался старым. Нужно заменить оба файла; актуальная копируемая версия скрипта также лежит в `outputs/krasnodar-rustore/RuStoreCatalog.txt`.

Проверка после исправления контекста поставщика: не накапливать предыдущую товарную строку как контекст. Иначе страна дублировалась, а память не извлекалась. Теперь одна строка каталога содержит ровно один флаг и одну страну; при нескольких флагах поставщика создаётся отдельная строка для каждой страны. Память без единицы измерения (`128`, `256`, `512`) нормализуется в `ГБ`, а `1TB`/`2TB` — в `ТБ`. Отсутствующий лист (например, `аксессуары` в таблице Краснодара) пропускается без остановки синхронизации остальных листов. Эмодзи перед заголовком iPad распознаются. Реальный снимок: 102 позиции — телефоны 65, айпады 8, наушники 9, пс 4; аксессуары 16 пропускаются, если листа нет. 12 автоматических тестов проходят.

## Рекомендуемая работа с GitHub

Создать **один приватный репозиторий**, например `priceflow-google-sheets`.

В нём хранить клиентские реализации отдельными папками верхнего уровня. Следующие три проекта добавлять по такому же принципу, не смешивая индивидуальные правила:

```text
Ульяновск/
  Telegram/
    README.md                  # правила клиента и установка
Москва/
  МойСклад/
    README.md                  # правила клиента и установка
<Новый город>/
  Telegram/ или МойСклад/
HANDOFF.md
```

Один репозиторий даёт общую историю, единые тесты и простую передачу в другой чат. Отдельные репозитории нужны только если продукты будут продаваться и развиваться полностью независимо разными командами.

## Как продолжить в новом чате Codex

Передать ссылку на репозиторий и написать:

> «Прочитай `HANDOFF.md` и `[город]/[интеграция]/README.md`. Работаем только с этим клиентом. Сначала проверь тесты и не меняй другой проект без отдельного запроса».

Так новый чат начнёт с актуальной структуры и не будет использовать устаревшие файлы из `outputs/`.

## Таблицы прайсов для внешней автозагрузки

Актуальные ссылки на таблицы, из которых внешняя автозагрузка читает цены:

- Орск / Яблочный Спас: <https://docs.google.com/spreadsheets/d/1mhTX7kvtxgv0zocTNsNl9-zsYBn5ToEHA_FP1-vD6XQ/edit?gid=0#gid=0>
- Краснодар / ru:Store: <https://docs.google.com/spreadsheets/d/1x6iJVSLZcdrwibfTFEroUn92qr_GpnY2griScOnElb4/edit?gid=0#gid=0>
- Москва: <https://docs.google.com/spreadsheets/d/1DvIyahnJKGUUJqE1soB-i1iXd1egzJ9BT_Rnr6c67Ns/edit?gid=1896727627#gid=1896727627>
- Электросталь / U-SE: <https://docs.google.com/spreadsheets/d/1aCROiDUeMYgLInNHTe6zakg01rGCo9CMLpCo5TJ7zZg/edit?gid=0#gid=0>
- Ульяновск / ProDevice: <https://docs.google.com/spreadsheets/d/1DbSLqiHzEm3qTl8DHIPGD9TnfRrjelfeDelGlrWmrO0/edit?gid=1408694438#gid=1408694438>

Эти ссылки не содержат токенов. Перед любыми изменениями нужно открыть именно соответствующую строку внешней автозагрузки и убедиться, что в поле «ЦЕНЫ» стоит тот же ID.

### ТЗ: единый контракт совместимости

Цель — пять таблиц одинаково читаются внешней автозагрузкой и не требуют городских исключений в её коде. Рабочее ядро листов должно быть одинаковым во всех городах:

```text
телефоны:
A Model | B SimConfig | C MemorySize | D Color | E Price | F [пустой разделитель]
G Model | H SimConfig | I MemorySize | J Color | K RamSize | L Price

аймаки, макбуки, айпады, часы, наушники, пс, дайсон:
Title | Price
```

В `телефоны` левый блок — только iPhone; `RamSize` у него отсутствует. Правый блок — только Android, где `RamSize` заполняется, только если это значение есть у поставщика. `Country`/`Страна` не создаётся. Между блоками ровно одна пустая колонка `F`; Android начинается в `G`, не в `I`.

Неизвестная поставщику SIM-конфигурация не подменяется выдуманным `SIM + eSIM`. В эталонном ручном прайсе для такого случая используется техническое значение `Не знаю`; применять его в конкретном городе можно только после проверки, что именно его ожидает принимающая автозагрузка. Модель, память, RAM и цвет пишутся раздельно; в `Model` нельзя помещать полное название с объёмом, цветом, флагом, рекламным текстом или ценой. Для листов `Title | Price` название очищается от технических хвостов поставщика (`true/false`, SEO-ключи, дубли характеристик, флаги страны), но важные статусы товара, согласованные с клиентом, сохраняются.

Цена — число в рублях без текста. Перед записью каждый импортёр должен валидировать шапки и останавливать пересборку с понятной ошибкой, если они отличаются от контракта; нельзя молча записывать данные в сдвинутый блок. В отчёте пересборки должны быть отдельные счётчики: записано, пропущено из-за отсутствующего листа, пропущено из-за неполной конфигурации.

### ТЗ по городам

- **Орск (RM Group).** Сохранить основной набор листов. Исправить разбор Android-конфигураций: в правом блоке должны быть модель, память и RAM отдельно (например, `Honor 400 8/256` не может становиться `MemorySize = 8 ГБ` с пустым `RamSize`). RM Group не передаёт SIM; до проверки контракта принимающей автозагрузки оставлять поле пустым, а `Не знаю` ставить только если оно требуется ей как явная техническая метка. Листы `аксессуары`, `камеры` и `колонки` удалены и больше не наполняются.
- **Краснодар (ru:Store).** Привести Android-модели к отдельному полю модели: `SAMSUNG S26 12/256 Black` не должен целиком попадать в `Model`; ожидается семейство `Galaxy S26`, память `256 ГБ`, RAM `12 ГБ`, цвет отдельно. Добавить валидацию стандартной шапки. Лист `аксессуары` удалён и больше не наполняется; `аймаки` и другие отсутствующие у поставщика категории не создавать с фиктивными строками.
- **Москва (МойСклад).** Очистить `Title` от повторов характеристик и служебных SEO-фрагментов поставщика; сейчас в названия попадают поисковые ключи и окончания `true/false`, что исключает точное сопоставление. Разобрать Android RAM/память только когда конфигурация явно есть. Лист `аксессуары` удалён и больше не наполняется; `аймаки` остаётся.
- **Электросталь (Telegram).** Исправить выбор блока телефонов: в правом Android-блоке сейчас встречается `iPhone 13`; все iPhone должны быть только слева. Дополнительные листы не добавлять.
- **Ульяновск (Telegram).** Переместить Android-блок из `I:N` в стандартный `G:L`, убрать две лишние пустые колонки. Нормализовать регистр моделей (`Galaxy A07`, не `Galaxy a07`) и чистить поставщицкие технические приставки/флаги из `Title`. Лист `аймаки` оставить; скрытый служебный лист `__PriceFlowTelegram` не является прайс-листом и должен игнорироваться внешней автозагрузкой.

### Порядок реализации после согласования

1. Зафиксировать общий шаблон на копии, не меняя действующие таблицы.
2. Не добавлять в прайсы категории вне обязательного ядра: аксессуары, камеры и колонки исключены.
3. Изменить пять импортёров и добавить тесты на точную шапку, положение Android-блока и ключи сопоставления.
4. Проверить на копиях: один iPhone, один Android и по одному товару из каждого обязательного листа.
5. Только после подтверждения заменить код в рабочих таблицах и включить пересборку.

### Выполнено по ТЗ (2026-08-24)

- В пяти рабочих таблицах физически приведён блок `телефоны` к `A:L`: Android перенесён в `G:L`, между блоками оставлена одна пустая колонка `F`. Шапки сверены чтением из каждой таблицы.
- Удалены не входящие в контракт листы: в Орске — `аксессуары`, `камеры`, `колонки`; в Краснодаре и Москве — `аксессуары`. Импортёры этих листов больше не создают и не заполняют.
- Все пять импортёров останавливают запись с понятной ошибкой при любой несовместимой шапке, вместо молчаливого смещения данных.
- Для отсутствующей в строке поставщика SIM во всех проектах записывается только техническая метка `Не знаю`; она не означает и не подменяет конфигурацию `SIM + eSIM`.
- Орск: `Honor 400 8/256` разбирается как модель `Honor 400`, RAM `8 ГБ`, память `256 ГБ`.
- Краснодар: `SAMSUNG S26 12/256` записывается как `Galaxy S26`, RAM и память — в отдельных полях.
- Москва: из отображаемых названий удаляются `#`, флаги страны и отдельные служебные слова `true`/`false`.
- Электросталь и Ульяновск: при записи в листы удаляются флаги из `Title`; маршрутизация телефонов сохраняет iPhone в левом блоке и Android в правом. Ульяновск нормализует `Galaxy A07`.
- Внешняя автозагрузка и её таблицы не менялись. Обновлённый Apps Script нужно заменить из текущих файлов репозитория/архивов и запустить одну ручную пересборку: в интерфейсе Google Sheets нет доступа к коду привязанного Apps Script через этот канал.

### Краснодар — последнее исправление (2026-08-22)

### Краснодар — прямая синхронизация цен в объявления (2026-08-24)

`Краснодар/RuStore/RuStoreCatalog.gs` больше не пересобирает ассортимент в таблице цен. По существующему триггеру раз в 15 минут он читает серверный read-only снимок `Прайс ru:Store новый` и обновляет **только** поле `Price` в листе мобильных объявлений `Краснодар | ru:Store` (таблица фиксирована в коде по ID таблицы и ID листа, поэтому пробел в имени вкладки не влияет на работу). Товары не создаются, не удаляются и не меняются.

Сопоставление консервативное: модель, память, цвет и SIM должны совпасть; для Android дополнительно требуется RAM. У iPhone RAM не является ключом, поскольку поставщик его не передаёт, а таблица объявлений содержит его техническое значение. Если у поставщика для одного ключа есть разные цены или позиция не найдена, цену не менять; позиция попадает в сохранённый отчёт `RUS_LAST_REPORT`. После замены `.gs` в привязанном Apps Script нажать одну ручную синхронизацию — она установит единственный триггер `syncRuStoreCatalog` на 15 минут.

Исправлен разбор актуального формата канала `Прайс ru:Store новый`:

- Строки из разделов `MacBook` и `Watch` теперь сохраняют семейство товара и попадают в листы `макбуки` и `часы`, включая цену.
- Артикул после цены (например, `66 990 MHFD4`) игнорируется как артикул; сама цена записывается. Строка с `?` после артикула по-прежнему не записывается.
- Удаляются служебные эмодзи поставщика из названий, в том числе `🔈` в AirPods. Флаги стран сохраняются.
- Добавлены цвета актуального прайса: Midnight, Starlight, Citrus, Blush, Indigo, Orange, Purple, Charcoal, Gray и Rose Gold.
- Реальный снимок проверен: телефоны — 67, макбуки — 7, айпады — 5, часы — 28, наушники — 9, PS — 4, аксессуары — 16. Все 15 автоматических тестов проходят.
- Раздел Dyson сейчас содержит только заголовок `Dyson` без строк с подтверждённой ценой. В лист ничего не добавлять до появления цены: это правило пользователя для Краснодара.

Для вставки в Apps Script использовать актуальный файл `outputs/krasnodar-rustore/RuStoreCatalog.txt` вместе с `Краснодар/RuStore/RuStoreCatalogSidebar.html`.

Дополнительная сверка 2026-08-22: в сообщении `iPhone Air` шесть позиций с подтверждённой ценой — они добавлены в разбор. В телефонах добавлены Lavender, Sage, Gold и Violet; в аксессуарах добавлены Camouflage, Pearl, Red и Volcanic Red. Поля цвета теперь заполнены для всех строк, где поставщик указал цвет. Пустыми допустимо оставить только `Подставка PS5` и `Дисковод PS5/PS5Pro`: это не варианты цвета. SIM-конфигурация заполняется исключительно когда она есть в строке поставщика; для iPhone 15/16 и аксессуаров поставщик её не пишет. Сверка точного текущего снимка с Google Sheet: телефоны — 73, аксессуары — 16, макбуки — 7, айпады — 5, часы — 28, наушники — 9, PS — 4, Dyson — 0. Dyson-пост остаётся заголовком без цен.

### Краснодар — расширение безопасного сопоставления цен (2026-08-24)

В прямой синхронизации цен добавлены только подтверждённые варианты одного товара в существующей таблице объявлений: `eSIM` = `Только eSIM`, `1 ТБ` = `1024 ГБ` (и `2 ТБ` = `2048 ГБ`), `Galaxy S26 Plus` = `Galaxy S26+`, а `синий` = `голубой`. Для линейки Galaxy S26 пустая у поставщика SIM-конфигурация сопоставляется с существующим вариантом `SIM + eSIM`, поскольку других S26-вариантов в целевой таблице нет. Новые товары по-прежнему не создаются и другие поля не меняются.

Одна строка остаётся намеренно без обновления: `iPhone 16 / 128 ГБ / голубой`. Поставщик присылает для неё две разные позиции (Teal — 63 490 ₽ и Ultramarine — 63 990 ₽), а обе в таблице объявлений обозначены одинаково как `голубой`. Скрипт не выбирает цену случайно; требуется отдельное бизнес-правило, какой из двух цветов соответствует этому объявлению.

### Ульяновск — прямая синхронизация цен в объявления (2026-08-25)

`Ульяновск/Telegram/TelegramCatalog.gs` после чтения Telegram, отбора минимальной цены по стране и применения клиентской наценки обновляет также только поле `Price` в фиксированном листе `Avito - Мобильные телефоны` рабочей таблицы объявлений Ульяновска. Триггер остаётся один — `syncTelegramCatalog` раз в 15 минут. Ассортимент, характеристики, тексты, остатки и любые другие поля объявлений не меняются.

Сопоставление консервативное: модель, память, цвет и SIM должны совпасть; для Android дополнительно совпадает RAM. Конфликтующие или не найденные позиции не получают цену и попадают в `TC_LAST_PRICE_REPORT`. Текущая исходная сверка до включения: в каталоге цен 793 строк телефонов, в таблице объявлений 527 строк, из которых буквальное безопасное совпадение есть только для 26. Остальные требуют не ослаблять ключ автоматически: это расхождения фактических характеристик и ассортимента, а не только регистра.

### Итоговая фиксация состояния (2026-08-25)

GitHub `main` содержит три последние внедрённые изменения: прямая синхронизация цен Краснодар (`180772e`), расширенное безопасное сопоставление Краснодар (`9749647`) и прямая синхронизация цен Ульяновск для листа мобильных телефонов (`1196cba`). Все изменения проверены соответствующими Node-тестами перед публикацией.

Ульяновск: внедрённый прямой обновлятор сознательно работает только с листом `Avito - Мобильные телефоны`. В рабочей таблице также существуют отдельные листы ноутбуков, планшетов, часов, наушников, приставок и Dyson, но их прямое сопоставление с Telegram-прайсом ещё не реализовано и не должно считаться включённым. Не расширять ключи для этих категорий эвристиками или по неполному названию: сначала для каждой категории нужен проверенный полный ключ конфигурации, после чего можно добавить обновление только `Price` и отдельные тесты.

Электросталь: изучены таблица настроек, прайс и рабочая таблица объявлений. Прямая синхронизация цен не добавлялась. В телефонах источника обнаружены несколько стран/цен для одной конфигурации, тогда как в объявлениях страна отсутствует; до выбора явного правила (минимальная цена, конкретная страна или пропуск) цену не обновлять.

### Расширение прямой синхронизации цен на все существующие листы (2026-08-25)

Ульяновск и Электросталь: прямой обновлятор цен расширен с листа мобильных телефонов на все существующие товарные листы их таблиц автообновления: телефоны, ноутбуки, планшеты, часы, наушники, игровые приставки и Dyson. Скрипт по-прежнему меняет только поле `Price`: объявления, остатки, характеристики, тексты, изображения и любые другие поля не затрагиваются.

Для телефонов остаётся строгий ключ: модель, память, цвет и SIM, а для Android также RAM. Для остальных категорий используется только нормализованное точное совпадение поля `Title`; различие в цене для одинакового исходного ключа считается конфликтом, поэтому цена не меняется. Неполные, отсутствующие и конфликтующие совпадения сохраняются по каждому листу в `TC_LAST_PRICE_REPORT` (Ульяновск) или `ES_TC_LAST_PRICE_REPORT` (Электросталь). Обновление выполняется пачками непрерывных ячеек и не создаёт/не удаляет строки.

Таблицы и идентификаторы: Ульяновск — `19GKgYl_RYR5Ezl6_L_bjIGkHmM2_vsWp5X1ZTV4rAF0`; Электросталь — `19Kj6HeZphLA-AgfpSKrKzn3rT1GwWSLAhd0GxCfpCPs`. В обеих целевых таблицах листа iMac нет, поэтому эта категория намеренно не обновляется. Краснодарская указанная таблица содержит только лист мобильных телефонов; до появления отдельных листов других категорий его прямой обновлятор сохраняется только для телефонов.

### Ульяновск — безопасное ослабление ключа телефонов (2026-08-25)

Сверка живых таблиц показала, что в автообновлении для части объявлений SIM записан техническим значением `Не знаю`, а у поставщика одна и та же модель/память/цвет может быть записана как `2 SIM` и `SIM + eSIM`. Скрипт теперь ослабляет ключ только если все подходящие строки поставщика с совпадающими моделью, памятью и цветом имеют одну и ту же цену. Если цвет также отличается, цена может быть взята только когда все строки поставщика с совпадающими моделью и памятью (а у Android также RAM) имеют одну цену. При двух разных ценах позиция по-прежнему не обновляется. Это не подмена конфигурации и не выбор «самой дешёвой» цены.

### Ульяновск — словарное сопоставление остальных категорий (2026-08-25)

Для iPad, Dyson, часов, MacBook, наушников и приставок сопоставление объявлений больше не требует равенства полного `Title`. Оно нормализует порядок слов и единицы памяти, удаляет рекламные/статусные слова и сравнивает значимые слова. Обязательно отсеиваются противоречия в ключевом железе: чип MacBook (`M4`/`M5`), RAM/SSD, серия Dyson (`HS`/`HD`), размер часов и семейство PS. Цена записывается только для единственного лучшего совпадения с одной ценой; одинаково близкие кандидаты с разными ценами сохраняются как конфликт. В текущей таблице автообновления Ульяновска нет листа iMac, поэтому iMac не может быть обновлён до добавления целевой вкладки.
# 2026-08-25 — Ульяновск: безопасное уточнение карточек для прямой синхронизации

- В `Ульяновск/Telegram/TelegramCatalog.gs` уточнено сравнение технических характеристик для прямой записи только в `Price` автозагрузки Avito.
- Форматы `16/512`, `16/512GB`, `16/1TB`, `128 ГБ` приведены к одному виду; для iPad различаются память и Wi‑Fi/LTE, для Watch — поколение SE/Ultra/Series, для AirPods — поколение Pro/обычной модели.
- Телефон поставщика без указанного цвета теперь остаётся доступен для уже существующего безопасного fallback: цена применяется лишь при единственной цене на модель/память/RAM. Это закрывает случай Galaxy S25 FE, но не разрешает конфликты цен.
- Комплектацию Dyson (кейс/без кейса), неполные PS-объявления и любые варианты с несколькими разными ценами код по-прежнему не выбирает автоматически.
- Добавлены тесты на эти правила. Хендов только дополнен; существующие разделы не менялись.
# 2026-08-25 — Ульяновск: автозагрузка объявлений теперь берёт готовый каталог

- В `Ульяновск/Telegram/TelegramCatalog.gs` источник для прямого обновления Avito изменён: после пересборки Telegram-данных цены берутся из уже заполненных вкладок самой таблицы `Ульяновск Автозагрузка`, а не из повторно распарсенных Telegram-названий.
- Для телефонов сверяются готовые поля `Model`, `SimConfig`, `MemorySize`, `Color`, `RamSize` и `Price` (iPhone не требует RAM); для остальных категорий — подготовленные `Title` и `Price`.
- Изменяется только `Price` в существующих листах объявлений. При дублирующемся точном ключе с разными ценами строка остаётся конфликтной, ничего не выбирается наугад.
- Добавлены тесты, подтверждающие перенос цены из готовых телефонных блоков и из Title/Price-источника. Хендов дополнен, прежние записи сохранены.
# 2026-08-25 — Ульяновск: порядок одной пересборки

- Перед переносом цен в таблицу объявлений вызывается `SpreadsheetApp.flush()`: прямой источник — только что записанные готовые вкладки `Ульяновск Автозагрузка`, а не отложенное состояние записи в той же сессии Apps Script.
- Хендов дополнен без замены прежних разделов.
# 2026-08-25 — Ульяновск: готовый каталог остаётся источником и при различии формата

- Второй этап по-прежнему читает только готовые вкладки `Ульяновск Автозагрузка`. Для телефонов после точного ключа допускается прежнее безопасное ослабление SIM/цвета исключительно при одной цене на модель/память/RAM; для остальных категорий применяется словарное сопоставление только к `Title` готового каталога, а не к Telegram-постам.
- Это сохраняет прямую цепочку «готовый каталог → объявления», но не теряет позиции из-за технической записи `Не знаю` или редакционного различия заголовков. Разные цены остаются конфликтом и не выбираются автоматически.
- Хендов дополнен, старые записи не заменялись.
# 2026-08-25 — Электросталь: перенос цен из готового каталога

- В `Электросталь/Telegram/TelegramCatalog.gs` второй этап синхронизации Avito переведён на ту же цепочку, что и Ульяновск: Telegram пересобирает готовую таблицу `Магазин-сервис U-SE/Электросталь Автозагрузка`, затем только из её готовых вкладок обновляется `Price` существующих объявлений.
- Перед чтением источника вызывается `SpreadsheetApp.flush()`. Телефоны используют готовые `Model`, `SimConfig`, `MemorySize`, `Color`, `RamSize`, `Price`; остальные категории — готовые `Title` и `Price`.
- При техническом различии SIM/цвета или редакционном различии Title допускается только безопасное, однозначное сопоставление в пределах готового каталога. Несколько разных цен для одного кандидата остаются конфликтом; поля кроме `Price` не меняются.
- Добавлены тесты переноса из готового каталога. Хендов дополнен, предыдущие записи сохранены.
# 2026-08-25 — Ульяновск и Электросталь: правило минимальной цены

- По явному правилу клиента в прямом переносе из готового каталога в объявления Ульяновска и Электростали несколько подходящих цен больше не считаются конфликтом: выбирается минимальная цена.
- Правило применяется к точным дублям и к допустимому сопоставлению по готовым полям/Title. Если подходящей позиции в каталоге нет, `Price` не меняется; по-прежнему меняется только `Price`.
- Добавлены тесты выбора минимальной цены для дублирующейся телефонной конфигурации. Хендов дополнен, существующие записи сохранены.
# 2026-08-25 — Электросталь: возврат к рабочей версии каталога

- По запросу клиента `Электросталь/Telegram/TelegramCatalog.gs` возвращён к версии до изменения источника синхронизации: структура и маршрутизация готового каталога не менялись.
- Единственная логическая правка в этой версии: если для одинакового ключа объявления поставщик прислал несколько цен, в `Price` выбирается минимальная. Для телефонов и Title-листов добавлены проверки.
- Хендов дополнен; прежние записи не удалялись.
# 2026-08-25 — Электросталь: iPhone всегда в левом блоке

- В восстановленной версии Электростали исправлена только маршрутизация телефонов с ведущим служебным символом (`•`, `·` и подобные) или пометкой `(Актив)/(Уценка)`: после удаления этого префикса iPhone направляется в левый блок, Android — в правый.
- Добавлен тест на `• iPhone` и `(Уценка) iPhone`. Правило минимальной цены для дублей сохранено; другие изменения логики не внесены.
- Хендов дополнен без удаления предыдущих разделов.

# 2026-08-25 — Электросталь: сопоставление телефонов по модели и памяти

- После точного совпадения `Model + MemorySize + Color + SimConfig (+ RamSize для Android)` для телефонов добавлен контролируемый fallback: совпадают `Model + MemorySize (+ RamSize для Android)`, а из отличающихся цветов/SIM выбирается минимальная цена поставщика.
- Это применяется только когда память (и RAM для Android) та же самая; строки с другой моделью или памятью не подменяются ценой другого товара.
- Добавлен тест: при одинаковых iPhone 15 128 ГБ с разными цветами/SIM в `Price` записывается минимальная цена. Хендов дополнен, предыдущие разделы сохранены.

# 2026-08-26 — Ульяновск: возвращено чтение всех правил наценки

- Выявлено, что код читал только `gid=0` (вкладка `Apple`) файла `Наценка`, хотя правила распределены по 14 вкладкам: Apple, Dyson, Samsung, Xiaomi/Poco, Honor, Huawei, Realme, Андроид, One Plus, Google, Яндекс, Экшн камеры, Наушники и колонки, Игровые приставки.
- Загрузка теперь читает все 14 вкладок и использует их конкретные правила. Сначала применяется точное правило товарной позиции, устойчивое к флагу страны, служебным пометкам и записи `GB/ГБ`; затем действуют прежние общие правила Apple/Samsung.
- Товары, для которых в правилах нет ни точной, ни общей строки, не получают выдуманную наценку. Добавлены тесты Galaxy и Dyson; 23/23 теста проходят. Хендов дополнен без удаления прошлых записей.

# 2026-08-26 — Электросталь: защита от старой цены и год в iPad-заголовке

- Причина неверной цены iPad Air 13 M3: в объявлении присутствует год в скобках (`(2025)`), которого нет в Telegram-прайсе; прежнее точное сопоставление не обновляло такую строку, оставляя старую цену.
- При сравнении Title год выпуска теперь игнорируется. Память, поколение/чип и остальные слова модели сохраняются, поэтому 128 ГБ не может сопоставиться с 256 ГБ.
- Если текущего товара у поставщика нет, старая цена очищается из `Price`, а не остаётся в объявлении и не может оказаться ниже закупочной. Электростальская шкала наценки Apple/Android и округление вверх до 500 ₽ сохранены; добавлены тесты на iPad и очистку отсутствующей позиции. 23/23 теста проходят.

# 2026-08-26 — Электросталь: точное сопоставление без года и защита рабочего кода

- Для всех Title-листов Электростали ключ сопоставления теперь не зависит от года выпуска, порядка слов, пометок `(Актив)/(Уценка)`, пунктуации, написания `Wi-Fi/eSIM` и записи памяти `2 ТБ/2048 ГБ`. Это устраняет ложные пропуски вроде `iPad 7 mini (2024)` ↔ `iPad Mini 7`.
- Ключ намеренно сохраняет все материальные признаки: модель/чип, память, цвет, RAM/SIM и специальные версии. Нельзя сопоставить `M4` с `M5`, `1 ТБ` с `2 ТБ` либо обычный iPad Pro с `Nano Texture`. При нескольких полностью одинаковых источниках сохраняется согласованное правило: в Price идёт минимальная цена.
- Перед каждой синхронизацией выполняются 26 регрессионных проверок. Они блокируют запись в таблицы, если будущая правка нарушает нормализацию, смешивает конфигурации или меняет утверждённую шкалу наценки/округление Электростали. Ручной запуск: `tcRunElectrostalRegressionTests`.

# 2026-08-26 — Электросталь: единое точное сопоставление всех категорий

- Телефоны продолжают сопоставляться по структурированным полям `Model + MemorySize + Color + SimConfig (+ RamSize для Android)`; при нескольких настоящих вариантах действует правило минимальной цены.
- Для MacBook, iPad, часов, наушников, Dyson и PS добавлен защищённый fallback для редакторски разных Title: год, порядок слов и служебные слова не препятствуют совпадению, но обязательны технические признаки. Несовпадение M-чипа, RAM/SSD, объёма iPad, LTE, Nano Texture, серии Dyson, размера/серии часов или семейства PS запрещает сопоставление и отправляет объявление в архив.
- В проверках добавлены реальные классы расхождений MacBook и Dyson и негативная проверка `обычный iPad Pro` ≠ `Nano Texture`. Fallback дополнительно запрещает связку разных брендов/семейств (Apple Watch ≠ Galaxy Watch), кодов Dyson и цветов. Наценка Электростали, её границы и округление не менялись.
- Важное ограничение жизненного цикла Avito: скрипт обновляет только `Price`. Пустая Price не снимает уже активное объявление в Avito. Чтобы убрать объявления отсутствующих SKU, нужно отдельное подтверждённое действие в используемом загрузчике Avito (снятие с публикации/остаток 0); не подставлять цену похожего товара.

# 2026-08-26 — Электросталь: актуальность объявлений обязательна

- Исправлено прежнее ограничение: для отсутствующей позиции больше недостаточно очистить `Price`. В каждой вкладке автозагрузки теперь обязательны `Price` и `DateEnd`.
- Если точного SKU нет в текущем прайсе поставщика, скрипт очищает Price и ставит `DateEnd` на вчера: объявление должно быть снято с публикации при следующей обработке фида Avito. Если тот же точный SKU вновь появляется, скрипт возвращает `DateEnd` на 30 дней вперёд и обновляет цену с наценкой.
- Это правило действует для телефонов и всех Title-категорий. Сходство названия не является основанием для публикации: чип, память, цвет и специальная версия продукта должны совпасть по защитному ключу.

# 2026-08-26 — Электросталь: очистка телефонного источника от аксессуаров

- Чехлы, бамперы, кошельки, MagSafe/Folio/Pencil, ремешки, кабели и зарядки исключены из телефонной категории: в проекте нет целевой вкладки объявлений для аксессуаров, поэтому они не могут стать «телефонами» с ложной ценой.
- Защита добавлена в регрессионные тесты; всего 27 обязательных проверок перед каждой синхронизацией.

# 2026-08-26 — Электросталь: Avito читает только готовый каталог

- После записи и `SpreadsheetApp.flush()` второй этап читает обратно готовые вкладки автозагрузки с уже применённой наценкой, а не временный результат разбора Telegram.
- Правило действует для телефонов, MacBook, iPad, часов, наушников, PS и Dyson. Отсутствующий SKU очищает `Price` и получает вчерашний `DateEnd`; вернувшийся SKU получает дату на 30 дней вперёд.
- Отсутствующая вкладка-источник останавливает выполнение с ошибкой, а не снимает массово объявления по догадке. Пустая существующая вкладка — явный нулевой ассортимент только своей категории.
